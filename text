package com.example.dmncompare;

import org.w3c.dom.*;
import javax.xml.parsers.DocumentBuilder;
import javax.xml.parsers.DocumentBuilderFactory;
import java.io.File;
import java.util.*;
import java.util.stream.Collectors;

public class CamundaVsJbpmRuleComparator {

    public static void main(String[] args) throws Exception {

        // ✅ Update your file paths
        String camundaFile = "src/main/resources/2FA_AA_RULES_ASYNCSMS_BAU.dmn"; // Camunda main
        String jbpmFile = "src/main/resources/2FA_AA_RULES_ASYNCSMS.dmn";        // jBPM to verify

        List<String> camundaRules = extractRules(camundaFile);
        List<String> jbpmRules = extractRules(jbpmFile);

        Set<String> camundaSet = new LinkedHashSet<>(camundaRules);
        Set<String> jbpmSet = new LinkedHashSet<>(jbpmRules);

        // ✅ Find rules missing in jBPM
        Set<String> missingInJbpm = new LinkedHashSet<>(camundaSet);
        missingInJbpm.removeAll(jbpmSet);

        System.out.println("=== ✅ Total Camunda Rules: " + camundaSet.size());
        System.out.println("=== 🟡 Total jBPM Rules: " + jbpmSet.size());
        System.out.println("=== 🔻 Missing in jBPM DMN ===");

        if (missingInJbpm.isEmpty()) {
            System.out.println("All rules are present in jBPM ✅");
        } else {
            missingInJbpm.forEach(System.out::println);
        }
    }

    // --- Extract <rule> entries from DMN ---
    private static List<String> extractRules(String filePath) throws Exception {
        DocumentBuilderFactory dbFactory = DocumentBuilderFactory.newInstance();
        dbFactory.setNamespaceAware(true);
        DocumentBuilder dBuilder = dbFactory.newDocumentBuilder();
        Document doc = dBuilder.parse(new File(filePath));
        doc.getDocumentElement().normalize();

        List<String> rules = new ArrayList<>();

        NodeList ruleNodes = doc.getElementsByTagNameNS("*", "rule");
        for (int i = 0; i < ruleNodes.getLength(); i++) {
            Element rule = (Element) ruleNodes.item(i);

            // --- Input entries ---
            NodeList inputEntries = rule.getElementsByTagNameNS("*", "inputEntry");
            List<String> inputs = new ArrayList<>();
            for (int j = 0; j < inputEntries.getLength(); j++) {
                Element in = (Element) inputEntries.item(j);
                inputs.add(extractText(in));
            }

            // --- Output entries ---
            NodeList outputEntries = rule.getElementsByTagNameNS("*", "outputEntry");
            List<String> outputs = new ArrayList<>();
            for (int j = 0; j < outputEntries.getLength(); j++) {
                Element out = (Element) outputEntries.item(j);
                outputs.add(extractText(out));
            }

            // --- Build comparable rule signature ---
            String key = String.join(" | ", inputs) + " => " + String.join(" | ", outputs);
            key = key.replaceAll("\\s+", " ").trim(); // normalize spaces
            rules.add(key);
        }

        return rules.stream().distinct().collect(Collectors.toList());
    }

    private static String extractText(Element element) {
        NodeList texts = element.getElementsByTagNameNS("*", "text");
        if (texts.getLength() > 0) {
            return texts.item(0).getTextContent().trim();
        }
        return "";
    }
}
